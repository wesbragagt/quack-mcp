{
  "name": "quack-mcp",
  "version": "1.0.0",
  "description": "MCP server for CSV analysis using DuckDB",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts",
    "dev": "node --experimental-strip-types --watch src/index.ts",
    "test": "node --experimental-strip-types --test tests/**/*.ts",
    "lint": "npx --yes oxlint@latest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "duckdb": "^1.1.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  },
  "bin": {
    "quack-mcp": "src/index.ts"
  },
  "keywords": [
    "mcp",
    "csv",
    "duckdb",
    "analysis"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=24.0.0"
  }
}
